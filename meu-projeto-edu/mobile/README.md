# Mobile App

This directory is for the Flutter mobile application.

## Setup

1. Install Flutter: https://flutter.dev/docs/get-started/install
2. Run `flutter create .` in this directory
3. Run `flutter run` to start the app